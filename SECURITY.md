# Security Policy

Do not open public issues containing API keys, exchange credentials, connection strings, account data, or exploitable security details. Revoke any credential that was exposed and replace it before continuing.

For dependency findings, include the affected package, resolved version, reproduction steps, and impact without attaching real secrets. Live exchange execution must remain disabled until keys without withdrawal permission, allowlists, capital limits, and explicit production approval are configured.
